// Generated by zerodds idl-ts. Do not edit.
//
// Source IDL:
//   struct Temperature { long celsius; string sensor_id; };
//
// Produced by `idlc ts temperature.idl -o gen/`: the mapped interface plus a
// `*TypeSupport` const fulfilling the DDS-TS TypeSupport contract
// (DDS-TS 1.0 §7, XCDR2-TS vendor spec §2–§3).

import type {
    DdsTopicType, EndianMode,
} from "@zerodds/cdr";
import {
    Xcdr2Writer, Xcdr2Reader,
} from "@zerodds/cdr";

export interface Temperature {
    celsius: number;
    sensor_id: string;
}

export function isTemperature(v: unknown): v is Temperature {
    if (typeof v !== "object" || v === null) return false;
    const o = v as Record<string, unknown>;
    if (typeof o.celsius !== "number") return false;
    if (typeof o.sensor_id !== "string") return false;
    return true;
}

export const TemperatureTypeSupport: DdsTopicType<Temperature> = {
    typeName: "Temperature",
    isKeyed: false,
    extensibility: "appendable",

    encode(s: Temperature, endian: EndianMode = "le"): Uint8Array {
        const w = new Xcdr2Writer(endian);
        this.encodeInto(w, s);
        return w.toBytes();
    },

    encodeInto(w: Xcdr2Writer, s: Temperature): void {
        w.writeInt32(s.celsius);
        w.writeString(s.sensor_id);
    },

    decode(bytes: Uint8Array, offset = 0, length = bytes.length - offset): Temperature {
        const r = new Xcdr2Reader(bytes, offset, length, "le");
        return this.decodeFrom(r);
    },

    decodeFrom(r: Xcdr2Reader): Temperature {
        const celsius = r.readInt32();
        const sensor_id = r.readString();
        return { celsius, sensor_id };
    },

    keyHash(_s: Temperature): Uint8Array {
        return new Uint8Array(16);
    },
};
