[package]
name = "rust-dev-install-hello"
version = "0.1.0"
edition = "2024"

# Empty [workspace] isolates this from the parent zerodds workspace.
[workspace]

[dependencies]
zerodds-dcps = { path = "../../crates/dcps" }
