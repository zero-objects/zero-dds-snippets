{
  "name": "zerodds-example-typescript-node-quickstart",
  "private": true,
  "type": "module",
  "description": "DDS-TS quickstart: bytes pub/sub roundtrip (hello.ts). Verifies the website §quickstart snippet against the real @zerodds/node types.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "tsx src/hello.ts"
  }
}
