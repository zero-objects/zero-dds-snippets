# Build artifacts and vendor codegen (regenerated by build.sh)
build/
gen/cyclone/
gen/fastdds/
# Keep gen/zerodds/secure_xtypes.hpp checked in: it is the idl-cpp reference
# output and lets the proof build without a local ZeroDDS idl-cpp toolchain.
